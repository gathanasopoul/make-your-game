export class Game {
    constructor(world, input) {
        this.world = world;
        this.input = input;
    }

    // Update game state
    update(dt) {
        void dt;
    }

    // Render game state
    render() {
    }
}